[build]
target = "index.html"
dist = "dist"
public_url = "/"

[serve]
port = 8080
open = false

[watch]
watch = ["src", "styles", "index.html"]

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "echo 'WASM build complete. Run trunk serve to start.'"]